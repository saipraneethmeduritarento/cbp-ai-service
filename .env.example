LOG_LEVEL="INFO"
ENVIRONMENT="local"                        # local | staging | production

# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL="postgresql+asyncpg://<user>:<password>@<host>:<port>/<dbname>"

# ── Google / Gemini ───────────────────────────────────────────────────────────
GOOGLE_PROJECT_ID="<gcp-project-id>"
GOOGLE_PROJECT_LOCATION="us-central1"
GOOGLE_PROJECT_LOCATION_GLOBAL="global"
GOOGLE_APPLICATION_CREDENTIALS=".creds/<service-account>.json"
GOOGLE_API_KEY="<gemini-api-key>"
GOOGLE_EMBEDDING_MODEL="gemini-embedding-2"
EMBEDDING_OUTPUT_DIMENSIONALITY=1536
GOOGLE_GENAI_USE_VERTEXAI=true
GEMINI_PRO_MODEL_NAME=""
GEMINI_FLASH_MODEL_NAME=""

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_DESIG_EMB_PREFIX="cbp_desig_emb"

# ── Designation Matching ──────────────────────────────────────────────────────
DESIGNATION_SIMILARITY_THRESHOLD=0.92     # 0.0 – 1.0 cosine similarity cutoff

# ── iGOT KB API ───────────────────────────────────────────────────────────────
KB_BASE_URL="https://<kb-host>"
KB_AUTH_TOKEN="Bearer <token>"

# ── JWT Auth ──────────────────────────────────────────────────────────────────
SECRET_KEY="<random-secret-key>"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440          # 24 hours
REFRESH_TOKEN_EXPIRE_DAYS=7

# ── Document Storage ──────────────────────────────────────────────────────────
DOCUMENT_STORAGE_TYPE=gcp                 # local | gcp
GCP_STORAGE_BUCKET=<bucket-name>
GCP_STORAGE_CREDENTIALS=".creds/<storage-service-account>.json"

# ── Notifications ─────────────────────────────────────────────────────────────
ENABLE_EMAIL_NOTIFICATION=False
NOTIFICATION_BASE_URL="https://<notification-service-host>"
SPV_PORTAL_URL="https://<spv-portal-host>"
MDO_PORTAL_URL="https://<mdo-portal-host>"

# ── Bhashini Translation ──────────────────────────────────────────────────────
BHASHINI_USER_ID=<bhashini-user-id>
BHASHINI_API_KEY=<bhashini-api-key>
BHASHINI_PIPELINE_ID=64392f96daac500b55c543cd
BHASHINI_CONFIG_API_URL=https://meity-auth.ulcacontrib.org/ulca/apis/v0/model/getModelsPipeline
BHASHINI_SUPPORTED_LANGUAGES=en,hi,te,kn,mr,ta,gu,ml,or,pa,bn,as

# ── Role mapping: Domain competencies from the Work Allocation Order (WAO) ─────
# When enabled, Domain competencies are derived per-designation directly from the raw WAO
# text (uncapped, exhaustive) instead of the summary-based capped list. Behavioural/Functional
# competencies are unchanged. Falls back silently to the existing behaviour if the raw WAO
# cannot be read. All optional — sensible defaults apply if omitted.
DOMAIN_FROM_WAO_ENABLED=true          # true | false  (default: true)
DOMAIN_FROM_WAO_CONCURRENCY=4         # max concurrent per-designation WAO calls (default: 4)
DOMAIN_FROM_WAO_MIN=6                 # min domain competencies per role; top up from summary if fewer; 0 disables floor (default: 6)
DOMAIN_FROM_WAO_CACHE_TTL_SECONDS=600 # TTL (s) for the Gemini context cache of the WAO PDF, reused across per-designation calls; 0 disables caching (default: 600)

# ── Observability (Langfuse) ── set LANGFUSE_ENABLED=true + keys to activate ──
LANGFUSE_ENABLED=false
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_SAMPLE_RATE=1.0
LANGFUSE_ENVIRONMENT=local   # segments traces (local|staging|production or app label); project itself = the key pair
